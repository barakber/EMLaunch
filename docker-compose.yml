services:
  emlaunch:
    build:
      context: .
      shm_size: '512m'
    image: emlaunch:latest
    container_name: emlaunch-notebook
    shm_size: '512m'
    ports:
      - "8888:8888"
    volumes:
      # Mount notebooks directory for live editing
      - ./notebooks:/app/notebooks
      # Optionally mount results directory
      - ./results:/app/results
    environment:
      - JULIA_NUM_THREADS=auto
    restart: unless-stopped
